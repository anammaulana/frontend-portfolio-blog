export interface Project {
    id: number
    title: string
    description: string
    imageUrl?: string
}

export interface BlogPost {
    id: number
    title: string
    content: string
    author: string
}
  